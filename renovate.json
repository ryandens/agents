{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", "helpers:pinGitHubActionDigests"],
  "lockFileMaintenance": {
    "description": "Re-resolve the lockfiles weekly so transitive deps stay current. The pep621 rule below only bumps packages declared in backend/pyproject.toml; lock-only deps pulled in through extras (requests via google-auth[requests], certifi, charset-normalizer) are invisible to that manager and otherwise sit at whatever version was resolved the last time something forced a full resolve.",
    "enabled": true,
    "schedule": ["before 4am on monday"]
  },
  "customManagers": [
    {
      "customType": "regex",
      "description": "Keep actionlint's version and its pinned download-script commit in lockstep with upstream GitHub release tags",
      "managerFilePatterns": ["/^\\.github/workflows/.+\\.ya?ml$/"],
      "matchStrings": [
        "ACTIONLINT_VERSION:\\s*(?<currentValue>\\S+)[\\s\\S]*?raw\\.githubusercontent\\.com/rhysd/actionlint/(?<currentDigest>[0-9a-f]{40})/"
      ],
      "depNameTemplate": "rhysd/actionlint",
      "datasourceTemplate": "github-tags",
      "extractVersionTemplate": "^v?(?<version>.+)$"
    },
    {
      "customType": "regex",
      "description": "Keep the pinned datadog-static-analyzer release in the CI workflow in lockstep with upstream GitHub release tags",
      "managerFilePatterns": ["/^\\.github/workflows/.+\\.ya?ml$/"],
      "matchStrings": ["DDSA_VERSION:\\s*(?<currentValue>\\S+)"],
      "depNameTemplate": "DataDog/datadog-static-analyzer",
      "datasourceTemplate": "github-releases",
      "extractVersionTemplate": "^v?(?<version>.+)$"
    },
    {
      "customType": "regex",
      "description": "Update explicitly annotated versions and optional digests in GitHub Actions workflows",
      "managerFilePatterns": ["/^\\.github/workflows/.+\\.ya?ml$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)(?: versioning=(?<versioning>\\S+))?\\s+\\w+:\\s*(?<currentValue>\\S+)(?:\\s+\\w+:\\s*(?<currentDigest>\\S+))?"
      ]
    }
  ],
  "customDatasources": {
    "opengrep-releases": {
      "defaultRegistryUrlTemplate": "https://api.github.com/repos/opengrep/opengrep/releases?per_page=100",
      "format": "json",
      "transformTemplates": [
        "{\"releases\": $map($, function($release) { {\"version\": $replace($release.tag_name, /^v/, \"\"), \"releaseTimestamp\": $release.published_at, \"changelogUrl\": $release.html_url, \"digest\": $replace($release.assets[name = \"opengrep_manylinux_x86\"][0].digest, /^sha256:/, \"\")} }), \"sourceUrl\": \"https://github.com/opengrep/opengrep\"}"
      ]
    }
  },
  "packageRules": [
    {
      "description": "Bump the >= floors in backend/pyproject.toml, which also regenerates uv.lock. The default rangeStrategy only edits a manifest when the new version escapes the declared range, and an open-ended '>=x' is satisfied by every release — so without this the backend gets no update PRs at all.",
      "matchManagers": ["pep621"],
      "rangeStrategy": "bump"
    },
    {
      "description": "Leave requires-python alone. The rangeStrategy 'bump' above rewrites it to the newest patch release ('>=3.14.6'), and Dependabot's uv updater ships a single patch per supported minor — so any patch-level floor makes its dependency-graph job abort with tool_version_not_supported. Python minor upgrades are a deliberate change across backend/.python-version and backend/Dockerfile anyway, so do them by hand.",
      "matchManagers": ["pep621"],
      "matchDepNames": ["python"],
      "enabled": false
    },
    {
      "description": "Hold eslint at 9. The plugins eslint-config-next bundles (eslint-plugin-react, -jsx-a11y, -import) all cap their peer eslint at ^9, so eslint 10 installs only because npm downgrades the nested peer conflict to a warning. Drop this rule once those plugins declare eslint 10 support.",
      "matchPackageNames": ["eslint"],
      "allowedVersions": "<10"
    },
    {
      "description": "Keep the AI SDK React bindings and core package on compatible releases.",
      "matchPackageNames": ["@ai-sdk/react", "ai"],
      "groupName": "AI SDK"
    }
  ]
}
